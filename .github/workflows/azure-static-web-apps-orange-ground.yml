# .github/workflows/azure-static-web-apps-orange-ground.yml
name: Azure Static Web Apps CI/CD – orange-ground-0b187a01e

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ORANGE_GROUND_0B187A01E }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"

          # 👇 Racine du projet Angular (là où il y a angular.json)
          app_location: "."

          # 👇 Pas d’API
          api_location: ""

          # 👇 Build Angular = dist/coreui-free-angular-admin-template/browser
          output_location: "dist/coreui-free-angular-admin-template/browser"